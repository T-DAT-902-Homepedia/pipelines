[project]
name = "homepedia-transport"
version = "0.1.0"
description = "Pipeline gold transport Homepedia : silver (parquet) -> tables PostGIS transport_commune / transport_stops"
requires-python = ">=3.13"
dependencies = [
    "pyarrow>=16.0.0",
    "psycopg[binary]>=3.1",
]
